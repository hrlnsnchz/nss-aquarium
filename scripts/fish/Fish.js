export const Fish = (fish) => {
    return `
        <section class="fish_card">
            <div><img  class="fish__image image--card" src="${fish.image}" /></div>
            <div class="fish__name">Name: ${fish.name}</div>
            <div class="fish__species">Species: ${fish.species}</div>
            <div class="fish__length">Length (inches): ${fish.length}</div>
            <div class="fish__habitat">Habitat: ${fish.habitat}</div>
            <div class="fish__diet">Diet: ${fish.food}</div>
        </section>
    `
}

export const HolyFish = (fish) => {
    return `
        <section class="fish_card">
            <div><img  class="fish__image image--card" src="${fish.image}" /></div>
            <div class="fish__name">Name: ${fish.name}</div>
            <div class="fish__species">Species: ${fish.species}</div>
            <div class="fish__length">Length (inches): ${fish.length}</div>
            <div class="fish__habitat">Habitat: ${fish.habitat}</div>
            <div class="fish__diet">Diet: ${fish.food}</div>
        </section>
    `
}

export const Soldiers = (fish) => {
    return `
        <section class="fish_card">
            <div><img  class="fish__image image--card" src="${fish.image}" /></div>
            <div class="fish__name">Name: ${fish.name}</div>
            <div class="fish__species">Species: ${fish.species}</div>
            <div class="fish__length">Length (inches): ${fish.length}</div>
            <div class="fish__habitat">Habitat: ${fish.habitat}</div>
            <div class="fish__diet">Diet: ${fish.food}</div>
        </section>
    `
}

export const Peasants = (fish) => {
    return `
        <section class="fish_card">
            <div><img  class="fish__image image--card" src="${fish.image}" /></div>
            <div class="fish__name">Name: ${fish.name}</div>
            <div class="fish__species">Species: ${fish.species}</div>
            <div class="fish__length">Length (inches): ${fish.length}</div>
            <div class="fish__habitat">Habitat: ${fish.habitat}</div>
            <div class="fish__diet">Diet: ${fish.food}</div>
        </section>
    `
}